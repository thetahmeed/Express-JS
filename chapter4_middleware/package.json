{
  "name": "chapter4_middleware",
  "version": "1.0.0",
  "description": "Middleware: Middleware is a function(s) which we can call between req and res.\r Example: User request > Middleware function (which is checking user is logged in or not) > response",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  }
}
